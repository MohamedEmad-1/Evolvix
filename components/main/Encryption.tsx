"use client";
import React from "react";

interface EncryptionProps {}

const Encryption: React.FC<EncryptionProps> = () => {
  return (
    <section id="focuselaiser" className="min-h-screen w-full flex items-center justify-center">
      <h1 className="text-4xl font-semibold">Focuselaiser</h1>
    </section>
  );
};

export default Encryption;
